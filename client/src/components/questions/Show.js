import React from "react";
import { Box, Container, CssBaseline, Typography } from '@material-ui/core';
import Button_Next from 'components/questions/button_next';
import Button_Prev from 'components/questions/button_next';

export default function QuestionContainer() {
  return (
    <React.Fragment>
      <CssBaseline />
      <Container maxWidth='md'>
      </Container>
    </React.Fragment>
  );
}